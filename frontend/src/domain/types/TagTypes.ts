// Re-export all tag domain types from shared package to avoid duplication
export * from '@gtd/shared'

// Frontend-specific tag types (if any future extensions are needed)
// Currently all tag domain types are now shared across frontend and backend