# Development overrides — exposes postgres port for local prisma commands
# This file is auto-loaded by docker compose alongside docker-compose.yml
services:
  postgres:
    ports:
      - "5432:5432"
